import XSSTemplate from "@/components/template/XSSTemplate";
import { Suspense } from "react";

const XXSPage = () => {
  return (
    <Suspense>
      <XSSTemplate />
    </Suspense>
  );
};

export default XXSPage;
